# Core
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
xgboost>=1.7.0
torch>=2.0.0
matplotlib>=3.6.0

# Survival analysis: Kaplan-Meier for the censoring distribution used by the
# IPCW weights, and Harrell's C-index.
lifelines>=0.27.0

# Notebooks
jupyter>=1.0.0
nbconvert>=7.0.0
nbformat>=5.7.0

# Optional. scikit-survival provides reference implementations of Uno's C and
# the integrated Brier score. src/evaluate.py uses it when importable and
# otherwise falls back to its own IPCW implementations, so it is not required.
# On Windows it needs a C++ toolchain to build its `ecos` dependency.
# scikit-survival>=0.22.0
